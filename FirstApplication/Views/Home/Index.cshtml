@model List<Person>

@{
    ViewBag.Title = "Home";
    var message = "Home - Index";
}

<h2>Index</h2>
<hr />
@Html.Partial("_HeaderMessage", message)
<br />

@foreach (var item in Model)
{
    <text>Id</text>
    @item.Id

    <text>Name</text>
    @Html.TextBoxFor(m => item.Name)

    <hr />
}